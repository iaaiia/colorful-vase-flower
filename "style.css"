body {
    background: linear-gradient(to bottom, #e3f0ff 0%, #aee1f9 100%);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.vase-container {
    position: relative;
    width: 230px;
    height: 400px;
    display: flex;
    justify-content: center;
    align-items: end;
}

.vase-neck {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    bottom: 160px;
    width: 60px;
    height: 50px;
    background: #bde4ef;
    border-radius: 40px 40px 30px 30px / 50px 50px 20px 20px;
    box-shadow: 0 6px 10px #4587ae33 inset;
    z-index: 1;
}

.vase-body {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    bottom: 20px;
    width: 120px;
    height: 170px;
    background: linear-gradient(135deg, #2196f3 70%, #90caf9 100%);
    border-radius: 50% 50% 45% 45%/60% 60% 100% 100%;
    box-shadow: 0 15px 30px #2196f330, 0 -4px 15px #fff5 inset;
}

/* Blume */
.blume {
    position: absolute;
    left: 50%;
    bottom: 180px;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    z-index: 2;
}

.stiel {
    width: 12px;
    height: 110px;
    background: linear-gradient(to right, #5fbf00 60%, #87d14d 100%);
    border-radius: 8px;
    box-shadow: 0 0 5px #5fbf0030;
    position: relative;
}

.blatt {
    width: 36px;
    height: 18px;
    background: linear-gradient(90deg, #4cd964 60%, #3ad29f 100%);
    border-radius: 36px 60px 14px 60px / 18px 36px 12px 36px;
    position: absolute;
    top: 50px;
    z-index: 2;
}

.blatt-links {
    left: -40px;
    transform: rotate(-25deg);
}

.blatt-rechts {
    right: -40px;
    transform: scaleX(-1) rotate(-25deg);
}

/* Blütenkopf */
.blume-kopf {
    width: 90px;
    height: 90px;
    position: absolute;
    top: -75px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
}

.blume-petal {
    position: absolute;
    width: 40px;
    height: 55px;
    border-radius: 50% 50% 50% 50% / 70% 80% 55% 55%;
    background: linear-gradient(135deg, #e040fb 60%, #f06292 100%);
    opacity: 0.92;
    z-index: 2;
}

.petal1 {
    left: 25px; top: -14px;
    background: linear-gradient(135deg, #e040fb, #ffd600 75%);
    transform: rotate(0deg);
}
.petal2 {
    left: 47px; top: 12px;
    background: linear-gradient(135deg, #03a9f4, #ffeb3b 75%);
    transform: rotate(65deg);
}
.petal3 {
    left: 42px; top: 44px;
    background: linear-gradient(135deg, #ff9800, #ff4081 80%);
    transform: rotate(130deg);
}
.petal4 {
    left: 5px; top: 50px;
    background: linear-gradient(135deg, #4caf50, #00bcd4 80%);
    transform: rotate(200deg);
}
.petal5 {
    left: -14px; top: 12px;
    background: linear-gradient(135deg, #f44336, #ffee58 80%);
    transform: rotate(295deg);
}

.blume-mitte {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 44px;
    height: 44px;
    background: radial-gradient(circle, #ffd600 75%, #fffde7 100%);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    border: 3px solid #fbc02d;
    box-shadow: 0 2px 12px #ffd60080 inset, 0 4px 15px #ffb30020;
    z-index: 4;
}